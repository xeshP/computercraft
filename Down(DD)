round = 0
if ... then
    numb = tonumber (...)
    while( numb > round ) do
        turtle.digDown()
        turtle.down()
        round = (round + 1)
        end
        
    else
        turtle.digDown()
        turtle.down()
        end
    
